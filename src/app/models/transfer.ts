export class Transfer {
    constructor(
        banco_id: number,
        id: number,
        correo: string,
        nombre: string,
        rut: string,
        tipo_cuenta: string,
        banco: string
    ) { }
}
